
log paths should be fully in settings - "{}/versions/vs-{}/logs" in artifactexporter.cpp
make sub configs in recording config struct - struct RecordingConfig should be separated to sub configs like in settings.hpp/cpp
should uniqueptr members be initialized with nullptr? change media recorder segment initalize with .get() for uniqueptr
reduce unique ptrs in media recorder if possible
in media recorder make audio capturer optional - change AudioCapturer to optional<AudioCapturer>
change response json fields to constexpr chars so there is not confusion
change app context to variant between the sync server or the media recorder instead of holding a struct of raw pointers
check whether clock sync actually worth it and works as expected - just recheck if functionality its ok

remove ClockMapper class since not using it
protocol export at should have differents names for clarity, also comment for better understanding
no need for enable_auto_start, just get media_recorder at contruction
change unix_now_ns name to system_clock_now_as_int64 or something even better 
check if Sync::steady_deadline_from_unix_ns is actually necesarry 
change dispatch in session
check if i can use unique ptr for msg buffer on send for sync and worker client
check if queue is empty only on do_write without double checking
add logs when cleanup/boost handler - check specific errors like connection and stuff like that
add comment for ping pong delay for better understanding
refactor do_read to separate concerncs - just handle error inside the function and all the string format should be at a separate function both for the client and server, make common or something
add logs for message handling in sync master for better understanding
use try_as when parsing json for shorter and more understandable code 
give better names for time t1/t0/t2 - so its clearer
remove seq since not using 
seg_ms is not necesarry but buffer duration is
no need for stop_at for gracefull shutdown since connection aborted could be used

move close function to schedule reconnect
reconnect timer and cmd timer can be the same object

change buffer duration to a clearer name - more like recording length second or something like that

remove sync plan since not using 

refactor sync client/server functions because they have ton of similiarities

check offset strategy (check both cases, client late / server late) - make sure its correct

check audio mixer jthread stop request and join - counter intuitive
rolling segments should have separate functions for create and remove tmp directories for better understanding

unify jthread throghout the project, dont use regular threads 

remove bug assemble (and the logic that combines the audio video, there is no need for it)

{{"type", "save_at"}, {"at", master_ns}, {"output_path", output_path}} - output path is not really necesarry 

in boost handlers when checking for error codes check if its asio::error::eof and if so just close the connection, and if needed for more like connection closed etc...

do you think i can use just one timer in sync worker client? 
